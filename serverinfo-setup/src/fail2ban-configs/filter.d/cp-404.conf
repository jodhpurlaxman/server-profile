[Definition]
# Match 404 errors in LiteSpeed/OpenLiteSpeed access logs
failregex = ^"?<HOST> -.*HTTP.*" 404 \d+
# Only ignore robots.txt and favicon.ico specifically
ignoreregex = ^"?<HOST> -.* "(GET|POST|HEAD) /(robots\.txt|favicon\.ico) HTTP.*" 404
